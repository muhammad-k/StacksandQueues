This is my practice with stacks and queues in Java, trying to gear up for interviews.

A quick word that my driver function may seem messy.
But the goal was to comment one big block and have users use the stack, or the queue, and switch inbetween by uncommenting the block.